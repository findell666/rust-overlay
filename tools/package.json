{
  "name": "rust-overlay-tools",
  "private": true,
  "type": "module",
  "description": "Build-time tools: extract the item database and icon fingerprints from the local Rust install.",
  "scripts": {
    "build-db": "node build-item-db.mjs"
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
